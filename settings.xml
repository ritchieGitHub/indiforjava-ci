<settings>
	<proxies>
	</proxies>
	<servers>
		<server>
			<id>sourceforge-deploy</id>
			<username>${env.SITE_DEPLOY_USERNAME}</username>
			<password>${env.SITE_DEPLOY_PASSWORD}</password>
		</server>
		<server>
			<id>sonatype-nexus-staging</id>
			<username>${env.CI_DEPLOY_USERNAME}</username>
			<password>${env.CI_DEPLOY_PASSWORD}</password>
		</server>
	</servers>
	<mirrors>
	</mirrors>
	<profiles>
	</profiles>
</settings>